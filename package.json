{
  "name": "school-trailer-network-scraper-render",
  "version": "6.0.0",
  "description": "Render-ready Express + Playwright scanner that captures proxy video URLs and returns working encoded proxy URLs.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "install-browser": "npx playwright install chromium",
    "test-sample": "node test-sample.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "playwright": "1.45.3"
  },
  "engines": {
    "node": ">=18"
  }
}
